# 0006. (mirrored)

ADR-0006 ("Lint profile policy") is shared policy between this repo
and the sibling `aozora` repo. The afm-side copy was removed on the
extraction (ADR-0010); the canonical statement lives at
`aozora/docs/adr/0004-lint-profile-policy.md`.

Both repos enforce the same `[workspace.lints]` and `just strict-code`
gates against the same rationale.
