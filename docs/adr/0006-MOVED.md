# 0006. (mirrored)

"Lint profile policy" is shared with the sibling `aozora` repo; the canonical
copy is `aozora/docs/adr/0004-lint-profile-policy.md`. Both repos enforce the
same `[workspace.lints]` and `just strict-code` gates.
