# 0004. (moved)

The original ADR-0004 ("Accent decomposition preparse") was a
parser-layer decision and moved to the sibling `aozora` repo when the
parser was extracted (afm ADR-0010).

It now lives at `aozora/docs/adr/0003-accent-decomposition-preparse.md`.

The original number on this side is preserved as a redirect stub so the
ADR sequence (0000..) does not develop confusing gaps.
