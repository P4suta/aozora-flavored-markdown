# 0008. (moved)

The original ADR-0008 ("Zero parser hooks — Aozora-first lexer") is
the architectural thesis of the lexer and the foundational ADR of the
sibling `aozora` repo. It moved on the extraction (ADR-0010).

It now lives at `aozora/docs/adr/0001-zero-parser-hooks.md`.
