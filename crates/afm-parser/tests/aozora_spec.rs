//! Aozora annotation spec — hand-written fixtures, one file per annotation category.
//!
//! Fixtures live in `spec/aozora/*.json` and pair an input snippet with the expected
//! AST. Each is small and self-contained so a failing case points directly at the
//! offending annotation kind.

#[test]
#[ignore = "M0 Spike — fixtures land in M1, parser wiring in M1/M2"]
fn aozora_notation_fixtures() {}
